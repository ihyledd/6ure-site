const { listYamlFilesInDir, getEditorFromYamlFile, LEAKS_DATA_PATH } = require("./.next/server/app/api/protection/links/yaml-files/route.js");
// wait, we can just compile a quick typescript file to test it.

